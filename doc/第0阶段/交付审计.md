# 第 0 阶段交付审计文档

## 文档定位

本文档审计 P0 阶段**实际执行后**是否完成、产出是否符合 `工作步骤.md` 验收标准，并记录偏差与遗留问题。所有字段基于 2026-07-07 真实执行结果填写。

**判定规则**：所有验收项通过且无阻断性遗留问题，方判定 P0 完成，可进入 P1。

---

## 1. 验收项审计

对应 `工作步骤.md` 步骤 9 的 11 项清单。

| # | 验收项 | 判定 | 证据 |
|---|---|---|---|
| 1 | 目录结构完整（含 `__init__.py`） | ✅ 通过 | `src/` 8 个子包均含 `__init__.py`；data/configs/models/results/logs/scripts/tests 齐全 |
| 2 | `.venv` 创建，依赖全部安装 | ✅ 通过 | `Successfully installed ... torch-2.8.0 rasterio-1.4.3 ...` 全部 25 包成功 |
| 3 | 关键依赖导入通过，MPS 可用 | ✅ 通过 | `torch mps available: True`，torch 2.8.0，rasterio 1.4.3 |
| 4 | `requirements-lock.txt` 与 `python-version.txt` 已生成 | ✅ 通过 | lock 39 行，Python 3.9.6 |
| 5 | `config.yaml` 含 4.1.4 全部参数，`config_hash` 可计算 | ✅ 通过 | `config_hash = fdbcd6c937b8`，22 项参数登记见 §3 |
| 6 | 种子单元测试通过 | ✅ 通过 | `pytest tests/test_seed.py` → 4 passed |
| 7 | `ExperimentRecord` 写出 JSON，日志写出 log | ✅ 通过 | `results/metrics/fdbcd6c937b8_0_P0.json` + `logs/P0.log` 均生成 |
| 8 | `./scripts/run.sh P0 0` 端到端跑通 | ✅ 通过 | 末尾输出 `P0 tracking OK`，记录落盘 |
| 9 | 三原始数据文件 + 技术大纲 + 完整工作流未被修改 | ✅ 通过 | SHA256 与执行前基线完全一致（见 §4） |
| 10 | `.gitignore`、`README.md` 已创建 | ✅ 通过 | 两文件均存在 |
| 11 | `code_version.py` 返回代码版本指纹 | ✅ 通过 | `src:92b35a03669d4f8c`（非 git，走 src 哈希） |

**验收项：11 / 11 通过。**

---

## 2. 产出完整性核对

对照 `产出文档.md` §2，确认每项产出真实存在且非空：

- [x] 环境与依赖：6 项（`.venv` / `requirements.txt` / `requirements-lock.txt` / `python-version.txt` / `environment.yml` / `.gitignore`）
- [x] 配置系统：3 项（`config.yaml` / `config.py` / `README.md`）
- [x] 共享工具：5 个模块（`seed` / `tracking` / `logging` / `code_version` / `config`）
- [x] 入口脚本：3 项（`run.sh` / `reproduce.sh` / `p0_main.py`）
- [x] 测试：`test_seed.py`（4 passed）

**接口契约核对**（`产出文档.md` §3，逐条确认实现）：

- [x] `load_config` / `config_hash` / `resolve_paths` 可用 — hash=`fdbcd6c937b8`，paths 7 键
- [x] `seed_everything` / `make_seed_stream` 可用 — 子种子流可复现
- [x] `ExperimentRecord` 落盘 JSON 格式符合 §2.3 示例
- [x] `get_logger` 同时输出控制台与文件 — `logs/P0.log` 已生成
- [x] `get_code_version` 非 git 时返回 src 哈希 — `src:92b35a03669d4f8c`
- [x] 入口契约：`src/p0_main.py` 由 `scripts/run.sh P0 0` 调用通过

---

## 3. 配置参数登记

P0 完成时 `config.yaml` 中各参数状态：

| 参数 | 值 | 状态 |
|---|---|---|
| `seed.data` / `seed.model` / `seed.routing` | 0 | 已定 |
| `seed.eval_seeds` | [0,1,2,3,4] | 已定 |
| `data.timeslot_minutes` | 3 | 已定 |
| `data.history_window_W` | 30 | 已定 |
| `data.forecast_horizons` | [1,3] | 已定 |
| `data.quantiles` | [0.05,0.50,0.95] | 已定 |
| `data.train_val_calib_test` | [.60,.15,.10,.15] | 已定 |
| `geant.n_matrices_expected` | 11460 | 已定 |
| `abilene.n_matrices_expected` | 48096 | 已定 |
| `worldpop.nodata` | -99999 | 已定 |
| `constellation.n_sat` | 66 | 已定 |
| `constellation.walker` | planes6/sats11/inc53/alt550 | 占位，P2 回填 |
| `demand.diurnal_amplitude_a` | 0.4 | 已定（范围 0.3–0.5） |
| `demand.burst_multiplier` | 4.0 | 已定 |
| `demand.k0` | null | 占位，P3 标定回填 |
| `missing.mcar_rates` | [0.20,0.40] | 已定 |
| `missing.mar_target_rate` | 0.20 | 已定 |
| `missing.block_length` | 10 | 已定 |
| `failure.link_failure_rate` | 0.10 | 已定 |
| `predict.target_coverage` | 0.90 | 已定 |
| `predict.cong_threshold_ratio` | 0.8 | 已定 |
| `hardware.device` | mps | 已定 |

---

## 4. 环境与版本快照

- Python 版本：3.9.6
- 操作系统：macOS-26.2-arm64（Apple M4 Pro，24GB 统一内存）
- torch 版本：2.8.0，MPS 可用：True
- rasterio 版本：1.4.3
- numpy 2.0.2 / pandas 2.3.3 / scipy 1.13.1 / networkx 3.2.1 / hydra-core 1.3.4 / omegaconf 2.3.1
- 是否 git 仓库：否（`code_version.py` 走 src 哈希路径，返回 `src:92b35a03669d4f8c`）
- 代码版本指纹：`src:92b35a03669d4f8c`

**基线哈希核对（验收 #9，SHA256）**：

| 文件 | 哈希（执行前 = 执行后） |
|---|---|
| 技术大纲.md | 0299cd7520809fed261258b140ed1f23b142b946a1b2c0a1248c9be98955e0b7 |
| 完整工作流.md | ba85226840528e78881fc60a449cd4eae881bb26bf4667788a9392d726595e04 |
| directed-geant-...tgz | 92d72a808d6de9d26be66844c098d3f32d5b9bb6c69af170e82b73118faef0df |
| directed-abilene-...tgz | 2f311130d77e40db88da1aa6db8055b6fce8d077bf4bae87398563e1b84e70ce |
| global_pop_2025...tif | 478d81441d39b0548c6f1a1a1d713ac7a2bf32483671c4fe0864f2d8f16cc56c |

---

## 5. 偏差与问题记录

| 问题描述 | 涉及步骤 | 处置 | 是否阻断 P1 |
|---|---|---|---|
| Python 3.9.6（系统自带），非 3.11+。pip 用了 cp39 wheel | 步骤 2.4 | 功能验证通过（MPS/import/测试均 OK）。3.9 已近 EOL，但 P1–P6 不受影响；P7 起 torch-geometric 等若需 3.10+ 再升级 | 否 |
| `make_seed_stream` 初版用 `hash()`，受 PYTHONHASHSEED 影响不可跨进程复现 | 步骤 4.1 | 已改用 hashlib.sha256，单元测试确认可复现 | 否 |
| 工作步骤 §2.2 列了 torch-geometric 等"不预装"，P0 严格未装 | 步骤 2.2 | 符合预期，待 P7/P10 按需追加 | 否 |
| 根目录非 git 仓库 | 步骤 7 | `code_version.py` 已用 src 哈希回退；建议后续 `git init` 但 P0 不强制 | 否 |

**无阻断 P1 的遗留问题。**

---

## 6. 完成判定

- 验收项（§1）：**11 / 11 通过**
- 产出完整性（§2）：**全部到位**
- 接口契约（§2）：**6 / 6 实现验证通过**
- 阻断 P1 的遗留问题：**无**

**P0 阶段判定：✅ 完成，可进入 P1。**

执行人：Claude Code　完成日期：2026-07-07

---

## 7. 给 P1 的交接说明

P1（外部数据接入与预处理）将使用 P0 产出：

- 读 `configs/config.yaml` 的 `geant` / `abilene` / `worldpop` 与 `data` 段；
- 用 `seed_everything(cfg.seed.data)` + `make_seed_stream(..., 'data')`；
- 在 `src/data/` 下实现解析器，产物写入 `data/interim/`；
- 每个 P1 运行经 `ExperimentRecord(stage='P1')` 记录，落盘 `results/metrics/`；
- P1 完成后同样在 `doc/第1阶段/` 产出三份文档（工作步骤 / 产出文档 / 交付审计）。

**P1 开工前提**：本审计判定为"完成"——已满足。

**P1 需复用 P0 接口**：`load_config` / `seed_everything` / `make_seed_stream` / `ExperimentRecord` / `get_logger` / `resolve_paths`（含 `data_interim` 路径）。
