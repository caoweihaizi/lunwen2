# 第 7 阶段产出文档：预测模型实现与训练

## 文档定位

登记 P7 阶段产出的预测模型与训练结果，供 P8/P9/P10 使用。所有数值基于 2026-07-08 真实训练（seed=0）。

---

## 1. 代码模块（`src/predict/`）

| 模块 | 用途 |
|---|---|
| `dataset.py` | 训练样本构建（稠密张量，396 全局边，采样 2000 窗/run） |
| `model.py` | PredictModel（GRU-D + GRU + 单调分位数头）+ Pinball Loss |
| `train.py` | 训练流程（Adam，早停，crossing 监控） |

入口：`src/p7_main.py`。测试：`test_predict.py`（4 测）。

---

## 2. 模型架构（技术大纲 §5.1-5.4，轻量版）

```
历史窗口 (B, W=30, 12) + 未来计划 (B, 3, 3)
        ↓
GRU-D 缺失恢复（§5.2）：γ=exp(-W·age)，缺失处用 (1-γ)·mean 恢复
        ↓
GRU(hidden=64) 编码时序
        ↓
拼接未来计划 MLP → decoder MLP
        ↓
单调分位数头：y05=softplus(z0), y50=y05+softplus(z1), y95=y50+softplus(z2)
        ↓
输出 (B, n_h=2, 3) [h=1,3 的 q05/q50/q95]
```

**特征（12维）**：carried/util/queue（observed）+ 3 mask + 3 age + delay/dist/active。

**关键简化（诚实记录）**：
- 技术大纲 §5.3 的节点 GAT 需整图消息传递，但 P7 按"单边样本"训练（每样本一条边），GAT 退化为边级 MLP。完整 GAT 在 P10 路由环境（候选邻居集合）实现更自然。
- 路由状态输入（§5.4 下一跳/分流比例）因 P4 keep_detail=False 未存明细，暂用 link_active + 物理属性近似。

---

## 3. 训练结果

| 项 | 值 |
|---|---|
| 训练数据 | 9 run × mcar20（多策略混合），18000 样本 |
| 验证集 | 4500 样本（val 段） |
| epochs | 50 |
| best_val_loss | **0.1473**（Pinball，标准化空间） |
| **crossing_rate** | **0.0**（单调头有效） |
| 参数量 | 204306 |
| 推理时间 | 6.49 ms/样本 |
| 训练耗时 | ~130s（2-3s/epoch） |

**checkpoint**：`models/predict/predict_v1.pt`（含 state_dict + normalize + config）。

**调参历程**：
- v1（hidden=64,1层,20epoch）：val_loss 0.2575——模型坍缩预测 0
- 诊断发现：单调头 `q05=softplus(z0)` 强制非负，但标准化标签 median=-0.255 有负值，模型无法拟合
- v2 修复（q05 可负，q50/q95 用 softplus 增量）+ hidden=128/2层GRU/50epoch/LR调度/梯度裁剪：val_loss 0.1473（改善 44%）
- v2 loss 0.137 < 恒定中位数 0.183，证明模型有效

---

## 4. 已知问题

| 问题 | 原因 | 处置 |
|---|---|---|
| ~~模型坍缩预测0~~ | 单调头 q05=softplus 强制非负，标签有负值 | **已修复**：q05 可负，q50/q95 用 softplus 增量 |
| 时序区分弱（q50 std=0.001，与标签相关0.07） | 模型学到预测中位数但未充分用时序区分高低流量 | P8 评价驱动：加 lag/attention 特征，或接受为方法初版 |
| GAT 退化为边级 MLP | 单边样本无图结构 | P10 路由环境实现完整 GAT |
| 路由状态未输入 | P4 未存明细 | 近似或 P4 重跑 keep_detail |

**val_loss 0.1473 优于恒定中位数 0.183**，模型有效但时序学习仍有提升空间。P8 会用 MAE/RMSE 等原始量纲指标评价，并与 Historical Average/LSTM/GCN-GRU 基线对比。

---

## 5. 给 P8/P9/P10 的接口

1. **checkpoint**：`torch.load('models/predict/predict_v1.pt')` → state_dict + normalize + config；
2. **加载模型**：`PredictModel(n_feat=12, hidden=64, n_h=2, h_max=3).load_state_dict(...)`；
3. **输入**：历史 W=30 时隙边特征 (12维) + 未来计划 (3,3)；
4. **输出**：(n_h=2, 3) 分位数 [h=1,3 的 q05/q50/q95]；
5. **标准化**：carried/util/queue 用 normalize mean/std（训练集估计）。

**P7 不交付**：CQR 校准（P9）、路由 RL（P10）、实验评价（P8）。P7 产出"冻结的预训练预测器 + 原始分位数"。
