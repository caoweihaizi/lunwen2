# 技术债盘点（P0–P9）与 P10 前置评估

## 文档定位

P10 启动前的整体盘点。梳理 P0–P9 所有已知简化/跳过/遗留，按"是否阻断 P10"分级，作为 P10 前置依据。

---

## 1. 技术债清单

### 🔴 阻断 P10（必须先处理）

**无。**

> **D1 降级说明**：原判断 D1（路由状态未输入）阻断 P10，经复核 §6.1 后修正——P10 actor 观测 `x_{ij,d,t}` 全是当前链路状态 + 预测器输出 + 势函数，路由策略在 P10 仿真时**在线产生**下一跳，不依赖 P4 预存路由明细。D1 仅影响 P7 预测器的路由状态输入（§5.4）和 P8 路由状态消融（§7.3.1），降为 🟡，P10 后续补。

### 🟡 影响质量但不阻断

| ID | 项 | 影响 P10 | 处置 |
|---|---|---|---|
| D2 | P7 GAT 退化为边级 MLP | P10 actor 自带 GAT，不依赖 P7 | P10 内实现，不阻断 |
| D3 | P4 e2e 时延=0 | 奖励用时延，但 P11 可重算 | P11 重算 |
| D4 | P4 明细表未存 | critic 用 link_state 聚合 | 不阻断 |
| D5 | P8 跳过项（路由状态消融/GCN-GRU/多缺失） | 路由状态消融需 D1 | 论文完善阶段 |
| D1 | 路由状态未输入预测器 | P10 actor 在线产生路由，不依赖；仅 P7 输入/P8 消融需 | P10 后补 |
| D6 | P9 q_hat≈0 / High-load 79% | 门控用区间宽度，可用 | 不阻断 |
| D7 | P7 时序区分弱（相关0.07） | 门控信号弱，但 P8 MAE 优 | 不阻断 |

### 🟢 无需处理（数据特性）

P1 OD 稀疏/缺口、P2 边数104/覆盖96%/9跳、P3 k0中位0——真实数据特性，8.2 已说明。

---

## 2. P10 前置缺口评估

**无阻断项。** D1 经复核降级（P10 actor 在线产生路由，不依赖 P4 预存）。

P10 可直接开始，分步推进：
1. POMDP + action masking + MAPPO 训练 + MUCAR/Point-MAPPO 两基线验证
2. 再扩到 6 基线（Dijkstra/ECMP/MAPPO无预测/Missing-aware Point-MAPPO）

D1（P4 路由明细 + P7 重训加路由状态）留待 P8 路由状态消融时补。

---

## 3. P10 风险提示

- P4 keep_detail 重跑可能再次内存膨胀（v1 教训）：需流式 + 仅 baseline 三策略
- P7 加路由状态后特征维度变，模型需调输入层
- P10 MAPPO 训练是工程最重，M4 Pro 24GB 需控制并行环境数/序列长度
- action masking 的势函数已在 P2 预计算（potential_sample），可用

---

## 4. 盘点结论

- P0–P9 整体健康，硬门控（P6）通过，预测（P7/P8）与校准（P9）核心贡献成立
- 唯一阻断 P10 的是 D1（路由状态），已决策完整处理（P4 重跑 + P7 重训）
- 其余技术债均不阻断，可在 P10 内绕过或论文完善阶段补
- P10 分步推进：先核心（POMDP+MAPPO+2基线），后扩展（6基线）

**盘点完成，可按 D1 处理路径推进 P10。**
